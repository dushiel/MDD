1. how can we describe the universe of all concepts?

2. build op concepts in the vocabulary that have their relation to all other concepts defined

3. syntactically explain all versions of a single relation with all concepts

4. visually this relates to moving or splitting the infinity

5. we can combine these first well-defined concepts to build up other well-defined concepts 

6. since the context types combine it corresponds to backus naur with the 5 different infinities.

7. instance and property truth / F0 S0 ?








to collapse to {1,0} syntactically we say:
 forall decisionB (-decisionB v decisionB)
 
 
 
 
 
 class variable which becomes true (is uncountability only possible with both truth styles?)
 
 
 
 1. observables are those concepts which we take as a given (input, we can observe "=" between them by assumption)
 2. concludables are this concepts which we build up from (they have a heuristical chance)
 3. identity law for property and elemental truth: -A v .. -> A and -A ^ .. -> A make  -A <-> A which is impossible due to identity law. seperatly they are possible, yet unevaluatable when removing the focus point (where observables are assumed).
 
 if you cannot observe an determining input what should the output be? 
 if you cannot observe a determining property, what should the output be?
 if we have a network, what will its output be for underdefined context - once wwe know what the missing node's evaluation is, we can give an ealuation of the concept.
 if we can add whatever new concept to the language whenever, we can always create impossibilities. (all other concepts are set to dontcare for A, we create a concept B which does care)
	Thus we should introduce a new evaluation type: dont-know. 
	
	 
 4. visualisation of building up our language: zooming in and out.

only caring about isomoprhic equality means we are discarding the observable "=" between the categories.


John voorbeeld: gegevens zijn nog geen informatie.
